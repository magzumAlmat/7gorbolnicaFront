export const metadata = {
  title: 'ЕСККС — АО «КазНИИСА»',
  description:
    'Единая система классификации и кодирования в строительстве.',
  alternates: { canonical: '/timso/registry' },
};

export default function Layout({ children }) {
  return children;
}
